#include<stdio.h>;

struct Heap {
    int elements[20];
};

void heapfyUp()
{
    
}

/*
    Complexidade: O(log n)
*/
void insert(struct Heap *heap, int valor) 
{
    
}

int main()
{
    struct Heap heap;
}